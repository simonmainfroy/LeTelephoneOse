<div class="form-group">
  <div class="searchbar-dashboard">
    <%= form_tag(dashboard_index_path, method: "get", remote: true) do %>
    <%= text_field_tag :search, params[:search], placeholder: "Utilisateur, artiste..." %>
    <%= submit_tag "Rechercher" %>
    <% end %>
  </div>
</div>
