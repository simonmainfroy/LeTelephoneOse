<div class="container">
  <div class="row">
    <div class="col-md-6 offset-md-3">
      <br><br>
      <%= simple_form_for(resource, as: resource_name, class: "pinkform", url: registration_path(resource_name)) do |f| %>
      <%= f.error_notification %>
      <h1 class="h3 mb-3 font-weight-normal text-center">S'enregistrer</h1>
      <div class="form-inputs">
        <%= f.label :email, "Email" %><br />
        <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control pinkform my-2" %>
      </div>
      <div class="form-inputs">
        <%= f.label :password, "Mot de passe" %>
        <% if @minimum_password_length %>
        <em>(<%= @minimum_password_length %> characters minimum)</em>
        <% end %><br />
        <%= f.password_field :password, autocomplete: "Mot de passe", class: "form-control pinkform my-2" %>
      </div>
      <div class="form-inputs">
        <%= f.label :password_confirmation, "Confirmer mot de passe" %><br />
        <%= f.password_field :password_confirmation, autocomplete: "Mot de passe", class: "form-control pinkform my-2" %>
      </div>
      <div class="actions mt-5">
        <%= f.submit "S'enregistrer", class: "btn btn-lg btn-pinkwhite btn-block" %>
      </div>
      <% end %>
      <%= render "devise/shared/links" %>
    </div>
  </div>
</div>
